---
description: Get one model's independent second opinion on a statement or decision
---

Use the `abe` validate MCP tool to get an independent second opinion on the statement below, then present the reviewer's take verbatim.

If the `abe` MCP tool is unavailable, fall back to running `abe validate "$ARGUMENTS"` via the shell and show its output.

Statement: $ARGUMENTS
